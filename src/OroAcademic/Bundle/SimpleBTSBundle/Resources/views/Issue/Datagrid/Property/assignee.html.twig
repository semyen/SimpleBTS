{% if resource_granted('oro_user_user_view') and (record.getValue('assigneeId') is not empty) %}
    <a href="{{ path('oro_user_view', {id: record.getValue('assigneeId')}) }}">{{ value }}</a>
{% else %}
    {{ value }}
{% endif %}
